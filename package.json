{
  "name": "case-labels-from-url",
  "version": "1.0.0",
  "description": "Generate PDF case labels from Truck Packer crewview links",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}",
    "dev": "serve -s . -l 3000"
  },
  "keywords": ["truck-packer", "case-labels", "pdf"],
  "license": "MIT",
  "dependencies": {
    "serve": "^14.2.5"
  }
}
